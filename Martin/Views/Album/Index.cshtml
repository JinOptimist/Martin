@model List<Dao.Model.Album>

@{
    ViewBag.Title = "Album";
    Layout = "~/Views/AdminMaster.cshtml";
}

<h2>Album list</h2>

@if (Model != null)
{
    foreach (var album in Model)
    {
        <p>@album.Id * @album.Name * @album.Description</p>
    }
}